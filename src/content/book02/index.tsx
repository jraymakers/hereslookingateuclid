import {
  PageDataList,
  parentPageData,
  parentPageDataNoNav,
} from '../../page';

import overview from './Book2Overview';

const pageData = parentPageData('II', 'Book II', [
  overview,
]);

export default pageData;
